# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# HTTP client for downloading files
httpx==0.26.0
aiofiles==23.2.1

# Speech-to-text
faster-whisper==1.0.0
requests>=2.31.0  # Required by faster-whisper

# Vocal separation (Demucs)
demucs>=4.0.0

# Utilities
numpy==1.26.4
pydantic==2.5.3
python-dotenv==1.0.0

# ====================
# VieNeu TTS (Local Vietnamese Text-to-Speech)
# ====================
# VieNeu SDK handles its own dependencies (torch, torchaudio, etc.)
# Install with: pip install vieneu --extra-index-url https://pnnbao97.github.io/llama-cpp-python-v0.3.16/cpu/
# Also requires eSpeak NG system package for phonemization.
vieneu>=1.2.0

# Audio processing (shared with SRT pipeline)
soundfile>=0.12.0
