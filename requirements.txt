# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# HTTP client for downloading files
httpx==0.26.0
aiofiles==23.2.1

# Speech-to-text
faster-whisper==1.0.0
requests>=2.31.0  # Required by faster-whisper

# Speech suppression uses FFmpeg's built-in arnndn filter (RNNoise-based)
# No Python dependencies required - uses FFmpeg's compiled filters

# Utilities
numpy<2.0.0
pydantic==2.5.3
python-dotenv==1.0.0

# ====================
# Valtec TTS (Local Vietnamese Text-to-Speech)
# ====================
# Note: valtec-tts is installed from local directory (./valtec-tts)
# See Dockerfile for installation instructions

# PyTorch (CPU version for VPS deployment)
torch>=2.0.0
torchaudio>=2.0.0

# Audio processing
scipy>=1.10.0
soundfile>=0.12.0
librosa>=0.9.0

# Text processing for Vietnamese
Unidecode>=1.3.0
num2words>=0.5.10
inflect>=6.0.0
viphoneme>=3.0.0
underthesea>=8.0.0
vinorm>=2.0.0

# Phonemization dependencies
cn2an>=0.5.20
jieba>=0.42.0
pypinyin>=0.44.0
jamo>=0.4.1
gruut>=2.4.0
g2p-en>=2.1.0
anyascii>=0.3.0
eng-to-ipa>=0.0.2

# Hugging Face Hub (for model auto-download)
huggingface_hub>=0.20.0

# Progress bar
tqdm>=4.60.0
