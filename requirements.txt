# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# HTTP client for downloading files
httpx==0.26.0
aiofiles==23.2.1

# Speech-to-text
faster-whisper==1.0.0

# Speech suppression (RNNoise)
# RNNoise is much lighter than Spleeter (~10x faster, ~50MB vs 1.5GB deps)
# Uses neural network trained specifically for speech/noise separation
rnnoise-python==1.0.0

# Utilities
numpy<2.0.0
pydantic==2.5.3
python-dotenv==1.0.0
